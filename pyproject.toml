[project]
name = "anno"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "treeparse",
]

[project.scripts]
anno = "anno:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

